module.exports = function(){
    var targetDone;
    
    var target = new Promise(done=>{
        targetDone = done;
    })

    targetDone.then = target.then;
    targetDone.catch = target.catch;
    
    return targetDone;
}